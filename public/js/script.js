document.addEventListener('DOMContentLoaded', function(){
	// let fir_sub = document.querySelector('.fir_sub');
	// console.log(fir_sub);

	// fir_sub.addEventListener("click", (e) => {
	//     e.preventDefault();

	//     let fir_inp = document.querySelector('.fir_inp').value;

	//     let request = new XMLHttpRequest();

	//     if (fir_inp !== '') {
	//     	request.open('POST', '/');
	//     	request.setRequestHeader("Content-type", "application/x-www-form-urlencoded");
	//     	request.send(`city=${fir_inp}`);
	//     }
	// });
});